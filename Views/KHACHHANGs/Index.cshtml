@model IEnumerable<DoAn1_BanFinal.Models.Khachhang>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.KhTen)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.KhDiachi)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.KhSdt)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.KhTen)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.KhDiachi)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.KhSdt)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.KhId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.KhId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.KhId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
