@model IEnumerable<DoAn1_BanFinal.Models.Quanly>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.QlTen)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.QlTen)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.QlId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.QlId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.QlId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
